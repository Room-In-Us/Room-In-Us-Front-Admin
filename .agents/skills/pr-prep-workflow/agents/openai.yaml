interface:
  display_name: "PR Prep Workflow"
  short_description: "Prepare PR text from the local diff."
  default_prompt: "$pr-prep-workflow Prepare a PR title, body, and test checklist from the current diff."
policy:
  allow_implicit_invocation: true
